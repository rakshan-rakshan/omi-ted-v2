# OMI-TED v2 — Session Handoff

---

## Session Identity
- **Session #**: 16
- **Machine**: kawin
- **Date/time started**: 2026-06-17 ~00:00 IST
- **Date/time ended**: 2026-06-17 ~02:30 IST
- **Context tokens used (approx)**: ~70%
- **Model used**: opencode/mimo-v2.5-free

## Repo State
- **Branch**: main
- **Last commit SHA before this session**: 20260506-15-kawin.md (session 15)
- **Uncommitted changes before final commit**:
```
 M backend/config.yaml
 M backend/config.py
 M backend/requirements.txt
 M backend/routers/search.py
 M backend/services/hybrid_search.py
 ?? backend/services/model_router.py
 ?? backend/services/query_rewriter.py
 ?? backend/services/context_builder.py
 ?? backend/services/reranker.py
 ?? backend/services/rag_pipeline.py
 ?? backend/tests/test_e2e.py
 ?? .handoffs/20260617-01-kawin.md
```
- **Stash entries**: none

## Session Goal
Build Open Notebook-inspired enhancements for OMI-TED v2's RAG system — multi-step RAG, query rewriting, reranking, context budget, OSS-first model routing, Telugu FTS fix.

## What Was Built (this session)
1. `services/model_router.py` — OSS-first model routing (Ollama → HF Inference → OpenRouter → skip)
2. `services/query_rewriter.py` — LLM query decomposition into structured search terms
3. `services/context_builder.py` — Priority-based context truncation with translation quality weighting
4. `services/reranker.py` — Cross-encoder reranking via sentence-transformers (local CPU)
5. `services/rag_pipeline.py` — Multi-step RAG pipeline (rewrite → parallel search → merge → rerank → context budget → synthesize)
6. Updated `config.yaml` — New `models:` and `rag_pipeline:` configuration sections
7. Updated `config.py` — New `_ModelSettings` and `_RAGPipelineSettings` Pydantic models
8. Updated `routers/search.py` — New `/ask/advanced`, `/models/health`, `/models/reranker` endpoints
9. Fixed `hybrid_search.py` — Changed PostgreSQL FTS from `'english'` to `'simple'` for Telugu support
10. Updated `requirements.txt` — Added sentence-transformers, httpx, pytest-asyncio
11. Created `tests/test_e2e.py` — 13 end-to-end tests for all new services

## Local Environment State
- **Backend running**: no — Docker not running
- **Frontend running**: no
- **DB in use**: PostgreSQL (Docker, not running)
- **Migration version**: unknown (Docker not started)
- **LLM provider in config.yaml**: ollama (local OSS-first routing)

## Files Created or Modified
| File | Status | Purpose |
|------|--------|---------|
| `backend/services/model_router.py` | NEW | OSS-first model routing with fallback chain |
| `backend/services/query_rewriter.py` | NEW | LLM query decomposition into search terms |
| `backend/services/context_builder.py` | NEW | Priority-based context truncation |
| `backend/services/reranker.py` | NEW | Cross-encoder reranking (local CPU) |
| `backend/services/rag_pipeline.py` | NEW | Multi-step RAG orchestrator |
| `backend/config.yaml` | MODIFIED | Added models: and rag_pipeline: sections |
| `backend/config.py` | MODIFIED | Added _ModelSettings, _RAGPipelineSettings |
| `backend/routers/search.py` | MODIFIED | Added /ask/advanced, /models/health, /models/reranker |
| `backend/services/hybrid_search.py` | MODIFIED | Fixed FTS for Telugu (english → simple) |
| `backend/requirements.txt` | MODIFIED | Added sentence-transformers, httpx, pytest-asyncio |
| `backend/tests/test_e2e.py` | NEW | 13 end-to-end tests |

## Tests Run
```
# Pending — Docker not running, tests cannot execute
cd backend
python -m pytest tests/test_e2e.py -v
```

## Verification
- All new service files compile without syntax errors
- No runtime verification possible (Docker/PostgreSQL unavailable)

## Known Issues
- Docker Desktop not running — tests cannot execute until started
- Google Cloud billing — ingestion paused at 74/7,139 videos
- Ollama reranker uses sentence-transformers directly (no native Ollama rerank API)

## What's Next
1. **Start Docker Desktop**, run migrations, run tests
2. Test Ollama integration locally with real queries
3. Begin ingestion resume (Google Cloud billing issue blocking)

## Blockers
- Docker Desktop must be running for PostgreSQL and Redis
- Google Cloud billing issue blocks video ingestion past 74/7,139

## Notes for Next Session
- The rag_pipeline.py orchestrates: rewrite → parallel search → merge → rerank → context budget → synthesize
- Model router tries Ollama first, falls back through HF Inference → OpenRouter → skip
- Telugu FTS fix uses `simple` config instead of `english` — verified by reading PostgreSQL docs
- sentence-transformers model downloads on first use (~500MB for cross-encoder)

---

## Pickup Protocol
1. `git pull --rebase origin main`
2. Read this handoff.
3. Start Docker Desktop and run:
```powershell
cd backend
docker compose up -d
python -m alembic upgrade head
python -m pytest tests/test_e2e.py -v
```
4. If tests pass, proceed with Ollama integration test.
