# OMI-TED v2 — Session Handoff

---

## Session Identity
- **Session #**: 18
- **Machine**: rakshan
- **Date/time started**: 2026-06-25 ~22:00 IST
- **Date/time ended**: 2026-06-26 ~07:00 IST
- **Context tokens used (approx)**: high (long session)
- **Model used**: claude-opus-4-8

## Repo State
- **Branch**: `codex/youtube-auto-translate-fallback`
- **Last commit SHA**: `1fe38e9`
- **Last commit message**: feat(ingest): route full fetch through proxy + configurable concurrency
- **Uncommitted changes**:
```
?? .claude/        (session config dir — intentionally not committed)
```
  Also untracked/local-only: `frontend/.env.local` (created this session), `backend/.env` (gitignored).
- **Stash entries**: none
- **5 new commits this session** (oldest→newest):
  - `dac52e5` fix(rag): resolve /ask/advanced hang, empty corpus, embedding mismatch, lang gate
  - `08616eb` feat(llm): config-driven OpenRouter translation+synthesis, paced ingest, bulk translate runner
  - `437ee32` fix(ingest): only back off on job timeouts, not isolated video failures
  - `1fe38e9` feat(ingest): route full fetch through proxy + configurable concurrency
  - (Not pushed to origin yet — push when ready.)

## Local Environment State (both RUNNING now)
- **Backend running**: YES — http://localhost:3001 (uvicorn, started with `YT_CONCURRENCY=1 YT_MAX_RETRIES=1`)
- **Frontend running**: YES — http://localhost:3000 (next dev, HTTP 200)
- **DB in use**: SQLite (`backend/dev.db`)
- **Row counts**: videos=713 (fetched=167, error=541, fetching=3, pending=2 ← last 5 are stale jobs from killed smoke tests, harmless), segments=33,746, messages=131, chunks=59, jobs=655, translation_cache=64, segments_with_en_auto=1,140
- **Translation provider (config.yaml `llm`)**: openrouter / `anthropic/claude-haiku-4.5`
- **RAG synthesis+rewrite (config.yaml `models`)**: openrouter / `anthropic/claude-haiku-4.5` (synthesis_provider/rewrite_provider=openrouter)
- **Sentry/PostHog**: no

## What I Just Completed
1. **RAG P0 fixes (verified live)** — `/ask/advanced` no longer hangs 180s (was a shared AsyncSession across `asyncio.gather`; now per-task sessions), query embeddings unified to e5-base (matches corpus), cross-lingual language gate removed, 30s endpoint timeout. Ran the corpus backfill (`build_chunks` + `embed_all_chunks`) → 131 messages / 59 chunks. `/ask/advanced` now answers in 8–15s.
2. **LLM-agnostic translation + synthesis** — provider is a config swap (`config.yaml`). Default OpenRouter `claude-haiku-4.5` for both. New `scripts/translate_segments.py` bulk-translates te→en via the agnostic `translate()` (per-task sessions, skips `[Music]` markers, cache race fixed). Validated: ~0.7s/seg, Biblical refs preserved.
3. **Ingest wiring + the real diagnosis** — updated yt-dlp 2024.12.13→2026.06.09, installed `socksio`, routed the WHOLE fetch (yt-dlp extract + json3) through the WARP proxy (was only the download), skip Strategy 1 under proxy, made `YT_CONCURRENCY`/`YT_MAX_RETRIES` env-tunable.

## What's Next (next session starts here) — THE 7K INGEST
- **Blocked on cookies.** Bulk YouTube ingest hits *"Sign in to confirm you're not a bot. Use --cookies"* on BOTH the home IP (429) and the WARP Cloudflare IP after a few hundred fetches. No proxy avoids it — **cookie auth is mandatory**.
- **First action when resuming:**
  1. Rakshan exports a `cookies.txt` from a **throwaway** YouTube account ("Get cookies.txt LOCALLY" extension or `yt-dlp --cookies-from-browser`).
  2. Set `YTDLP_COOKIES_FILE=<path>` in `backend/.env`; let both IPs cool first (hours).
  3. Run `python -m scripts.batch_ingest` (idempotent — retries the ~541 error rows + remaining backlog). Recommended env: `YT_MAX_RETRIES=1`; try direct first (comment `YT_PROXY` in `.env`) since it's fastest with auth, WARP as fallback.
- **After ingest:** run `scripts/translate_segments.py` (OpenRouter), then re-run `build_chunks` + `embed_all_chunks` to grow the RAG corpus.
- **Test command to verify pickup:**
```powershell
cd backend; .\venv\Scripts\python.exe -m uvicorn main:app --host 127.0.0.1 --port 3001
# in another shell: cd frontend; npm run dev
```

## Blockers
- **Bulk ingest needs a cookies.txt** (throwaway Google account) — see above. This is the one human action required.
- Both home + WARP IPs are throttled from today's diagnostic testing — needs a cooldown before any real run.
- Branch not pushed to origin.

## Notes for Next Session
- `backend/.env`: `YT_PROXY` (WARP SOCKS5 :40000) is ENABLED; `socksio` is installed so it no longer crashes. `OPENROUTER_API_KEY` was topped up + rotated this session and is funded/working.
- WARP gives a Cloudflare IP that's still geo-India (captions identical) and is ~2–3s/video when fresh — but it is NOT immune to the bot-wall.
- The ~541 "error" video rows are mostly false errors (socksio crash + bot-wall + caption-less); the idempotent re-run with cookies will reclaim them.
- Don't trust the green e2e tests for RAG — verify `/ask/advanced` live (the P0s were invisible to mocked tests).
- User asked to stop active work after starting the services; they will trigger the ingest run themselves and report errors.

---

## Pickup Protocol
1. `git checkout codex/youtube-auto-translate-fallback` (work is here, not main)
2. Read this handoff + `project_youtube_throttle` / `project_llm_agnostic_layer` memories
3. Get a `cookies.txt`, set `YTDLP_COOKIES_FILE`, let IPs cool
4. Boot backend + frontend (commands above), then run `scripts/batch_ingest`
5. Begin
