# OMI-TED v2 — Session Handoff

## Session Identity
- **Session #**: 20260630-01
- **Machine**: rakshan
- **Date**: 2026-06-30
- **Model used**: claude-opus-4-8
- **Theme**: glossary seeding + translation UX/robustness + **free-models-only** correction

## Repo State
- **Branch**: `codex/youtube-auto-translate-fallback`
- **Last commit SHA**: `e316fe6e2fe9e3ab344f71a609f3a02013b4ce6a`
- **Last commit message**: `feat(glossary): seed from unfoldingWord te_tw + inject glossary into translation`
- **Ahead of origin**: 4 commits, **NOT pushed** — `aba8c62` feat(backend), `c03d687` feat(frontend), `ac70b2f` docs(handoffs), `e316fe6` feat(glossary).
- **Uncommitted changes** (this session, after e316fe6):
```
 M backend/config.yaml
 M backend/database.py
 M backend/routers/batch.py
 M backend/routers/translate_runner.py
 M backend/services/translate.py
 M frontend/app/dashboard/page.tsx
 M frontend/app/editor/[videoId]/page.tsx
 M frontend/app/layout.tsx
 M frontend/app/removed/page.tsx
 M frontend/app/translate/page.tsx
?? .claude/          (leave uncommitted)
?? graphify-out/     (generated artifact — leave uncommitted; consider gitignoring)
```
- **Stash**: none

## Local Environment State
- **Backend running**: YES — **port 3001** (NOT 8000), venv uvicorn, **no --reload**, launched detached via WMI, logs at `…\scratchpad\uvicorn.{out,err}.log`. Health: `GET http://127.0.0.1:3001/health`.
- **Frontend running**: YES — port 3000 (`next dev`), launched detached via WMI, logs `…\scratchpad\next.{out,err}.log`.
- **DB**: SQLite `backend/dev.db` — now **WAL mode + busy_timeout=30000** (set in database.py).
- **Migration**: `009 (head)` (excluded_scope recycle-bin columns).
- **Row counts**: videos=1750, fetched=1510, segments=289,379, segs_translated=73,891, jobs=2339, glossary=**928**, translation_costs=40, translation_errors=26, excluded(recycle-bin)=2.
- **LLM provider/model**: openrouter / **`google/gemma-4-31b-it:free`** (FREE — see rule below).

## What I Just Completed
**Committed (4 commits above):** /translate page + cost/batch/error engine (migrations 007/008), /errors page (Ingest+Translation tabs, TranslationErrorLog), /removed recycle-bin (migration 009), and the **glossary seed** — 36→928 terms imported from **unfoldingWord translationWords Telugu** via `backend/scripts/import_uw_glossary.py`, plus **glossary-into-prompt injection** (`services/glossary_prompt.py`, threaded through translate/batch).

**Uncommitted (needs a commit):**
1. **FREE MODELS ONLY fix** — switched every translation default from paid → `google/gemma-4-31b-it:free`: `config.yaml` `llm.model`, fallbacks in `services/translate.py` (×2) + `routers/translate_runner.py` + `routers/batch.py` (TranslateRequest + translate_video_segments), and the editor default. (Prior sessions defaulted to paid `claude-haiku-4.5`; ~$0.06 was spent before the fix.)
2. **500-under-load hardening** — `database.py`: SQLite WAL + `busy_timeout=30000` (verified live) so concurrent writers wait instead of "database is locked". `routers/batch.py`: wrapped the non-YouTube path (→ clean 503, not 500) and made the cost/error-log write non-fatal.
3. **Editor model picker** — `editor/[videoId]/page.tsx` now uses the full `/api/v1/models` catalog (340) instead of 4 hardcoded presets.
4. **Recycle Bin UX** — `layout.tsx` NAV "Removed"→"Recycle Bin"; `removed/page.tsx` H1; visible red "🗑 Remove" (+ tooltip) on dashboard & translate rows (was faint gray).
5. **Translate headline cards** — hero "Segs done" card + long-fraction overflow fix (`translate/page.tsx`).

Verified: all 3 providers translate 200 on single calls; free default confirmed in-message; WAL+busy_timeout active; type-check clean; all pages render 200.

## What's Next (start here)
1. **Commit the uncommitted work.** Suggested split:
   - `fix(translate): default to free models only; never paid` (config.yaml, translate.py, translate_runner.py, batch.py model defaults, editor default)
   - `fix(backend): prevent translation 500s under load (SQLite WAL+busy_timeout, guard endpoint)` (database.py, batch.py guards)
   - `feat(editor): full model catalog for auto-translate` (editor)
   - `fix(ui): Recycle Bin rename, visible Remove, hero Segs-done card` (layout, removed, dashboard, translate)
2. **Decide: push** the branch (4+ commits) to origin?
3. **OpenRouter free tier is 429-rate-limited.** For bulk work, lower `llm.concurrency` (5→2) / `translate_run.video_concurrency` (2→1), OR use the local **`indictrans2`** provider (config block present; free + no rate limit — verify it's installed).

## Blockers / Decisions for Rakshan
- **Still-paid models NOT changed (not "translation", flagged):** `config.yaml` `rag.generation_model` (`claude-3-haiku`) and `models.openrouter_model` (`claude-haiku-4.5`) — power RAG Q&A synthesis **and the editor "Generate glossary" extraction**. Switch these to free too? (RAG answer quality may dip.)
- Free-tier 429 vs. local indictrans2 for bulk translation — which path?

## Notes for Next Session
- **STANDING RULE: translation = FREE models only, never paid.** (Global memory: `feedback_free_models_only.md`.)
- **Backend is :3001, not 8000.** `venv/Scripts/python.exe` is a **launcher shim** → one healthy server shows as **2 python processes** (venv parent + Python312 child); they're ONE server — don't kill the "system" one.
- `config.yaml` is read live (`_cfg()` re-reads each call) → config-only changes need no restart; **code changes need a backend restart** (no --reload).
- Clean-restart recipe (proven): kill all `uvicorn|multiprocessing.spawn` python → poll until `/health` fails (port free) → relaunch venv uvicorn via WMI with logs to scratchpad → poll `/health`. Frontend: `cmd /c npm run dev` via WMI from `frontend/`.
- Deferred (plan `~/.claude/plans/gleaming-napping-seal.md`): corpus→LLM **sermon-register** glossary pass (colloquial vocab no dataset has, ~$0.30 — but must use free model now); `bible-uedin`+WEB name alignment; in-app "Build seed from corpus" button.
- Import script re-runs safely: `python scripts/import_uw_glossary.py [--commit] [--include-other]` (idempotent upsert; `--include-other` adds ~1300 general terms).

---

## Pickup Protocol
1. Read this file.
2. Confirm servers: `GET http://127.0.0.1:3001/health` (200) and `http://127.0.0.1:3000` (200). If down, use the clean-restart recipe above.
3. `git status` — expect the 10 uncommitted files above. First action: commit them (step 1 in "What's Next").
4. Remember: **free models only** for translation.
