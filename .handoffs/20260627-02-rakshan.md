# OMI-TED v2 — Session Handoff

---

## Session Identity
- **Session #**: 21
- **Machine**: rakshan
- **Date/time started**: 2026-06-27 (afternoon IST)
- **Date/time ended**: 2026-06-27 IST
- **Model used**: claude-opus-4-8
- **Scope**: Built 3 next-phase features — Bulk Translate (Phase 0A), Advanced RAG UI + timestamped citations (Phase 1A/1B), Notebooks backend (Phase 1C). All verified locally. **Nothing committed yet.**

## Repo State
- **Branch**: `codex/youtube-auto-translate-fallback`
- **Last commit SHA**: `1fe38e9` (UNCHANGED — nothing committed this session, same as sessions 19/20)
- **Last commit message**: `feat(ingest): route full fetch through proxy + configurable concurrency`
- **Uncommitted changes** — ⚠️ working tree now mixes **session-19 work** with **this session's (21)**. Tagged below:
```
 M backend/main.py                      [19+21] registered translate_runner + notebooks routers
 M backend/models.py                    [19+21] s19=glossary.meanings; s21=Chunk.start_time, Notebook, NotebookMessage
 M backend/routers/batch.py             [21] extracted translate_video_segments() helper
 M backend/routers/glossary.py          [19]
 M backend/routers/search.py            [21] start_time + notebook_id + _resolve_filters
 M backend/routers/videos.py            [19]
 M backend/scripts/build_chunks.py      [21] populate chunk.start_time
 M backend/services/chunking.py         [21] ChunkResult.start_time
 M backend/services/hybrid_search.py    [21] start_time in 3 paths + SQLite bugfix + message_ids filter
 M backend/services/rag_pipeline.py     [21] _build_citations: start_time + chunk_text
 M frontend/app/editor/[videoId]/page.tsx   [19]
 M frontend/app/globals.css                 [19]
 M frontend/app/glossary/page.tsx           [19]
 M frontend/app/layout.tsx                  [19]
 M frontend/app/page.tsx                    [19]
 M frontend/app/search/page.tsx         [21] Ask+ (advanced RAG) mode + timestamp deep-links
 M frontend/app/stats/page.tsx              [19]
?? backend/alembic/versions/004_glossary_meanings.py   [19]
?? backend/alembic/versions/005_chunk_timing.py        [21] chunks.start_time
?? backend/alembic/versions/006_notebooks.py           [21] notebooks + notebook_messages
?? backend/routers/dashboard.py            [19]
?? backend/routers/translate_runner.py     [21] bulk translate run (paced, stoppable)
?? backend/routers/notebooks.py            [21] notebook CRUD + scoping helper
?? backend/services/glossary_extract.py    [19]
?? frontend/app/dashboard/                  [19 page + 21 edits] s21 added the Translate run control
?? frontend/components/FloatingMenu.tsx     [19]
?? frontend/components/GlossaryTools.tsx    [19]
?? frontend/hooks/                          [19]
?? .claude/  ?? .handoffs/*.md
```

## Local Environment State
- **Backend running**: NO (my smoke-test servers on :8011 were stopped + cleaned up). A prior :3001 instance may or may not still be up.
- **Frontend running**: unknown (not started/stopped by me this session)
- **DB**: SQLite `backend/dev.db`
- **Alembic head**: `006` (head) — ⚠️ I applied **005 + 006** to local dev.db this session. On any other machine / fresh DB, run `alembic upgrade head`.
- **Row counts** (live — ingest/translation appears to be actively progressing, numbers climbing):
  - videos=717 (fetched=315)
  - segments=69,620 · **en_final=7,177** · reviewed=0
  - messages=131 · chunks=59 (NOT reindexed yet — `chunks_with_start_time=0`)
  - glossary=3 · notebooks=0
- **LLM provider in config.yaml**: openrouter / `anthropic/claude-haiku-4.5`

## What I Just Completed (all verified locally, no API spend where avoidable)

### Phase 0A — Bulk Translate feature
Translate ingested videos from the dashboard (paced, stoppable, live progress) instead of a CLI script. Mirrors the existing ingest-dashboard run pattern.
- `backend/routers/batch.py` — extracted `translate_video_segments(video, *, session, provider, model, force, concurrency, segment_ids)` from the endpoint (endpoint behavior unchanged).
- `backend/routers/translate_runner.py` (NEW) — `_tr_run_state`/`_tr_run_loop`, one run at a time, paced by `TRANSLATE_PACE` (env, default 1.5s). Endpoints: `POST /api/v1/batch/translate/run` (mode `pending|all|ids`), `GET .../run/status`, `POST .../run/stop`.
- `frontend/app/dashboard/page.tsx` — gold "Translate pending" / "Re-translate all" / Stop control with progress.
- **Verified**: routes register; target queries correct on real DB; live HTTP lifecycle (start→complete→stop) resolved openrouter/haiku from config, 0-target run, no spend.

### Phase 1A/1B — Advanced RAG in the UI + timestamped citations
- `frontend/app/search/page.tsx` — new **Ask+** mode hitting `/api/v1/ask/advanced` (renders answer, rewritten search-term chips, sources). Timestamp deep-links (`...&t=Ns`, "Watch at 12:34") on search results, basic-ask sources, and advanced sources.
- `Chunk.start_time` (`models.py`) + migration `005_chunk_timing.py`. `chunking.py` computes min-segment start; `build_chunks.py` stores it; threaded through `hybrid_search.py` (3 paths), `rag_pipeline.py` `_build_citations`, `search.py` response models + all 3 endpoints.
- **🐞 Bug fixed in `hybrid_search.py` SQLite path** (the path used locally): results were reading `chunk_id`/`chunk_text`/`message_id` from a **stale leftover loop variable** (every result shared one chunk's id/text), and `youtube_id`↔`channel` were **swapped** (broken YouTube links). Now sourced correctly from `item_map[cid]`. Also fixed advanced-mode citations returning empty `chunk_text`. **Verified live**: distinct chunk_ids, correct `youtube_id=AJyvW7jToio` / `channel=Ophir Ministries`.

### Phase 1C — Notebooks backend ("NotebookLM" collections)
- `Notebook` + `NotebookMessage` (`models.py`) + migration `006_notebooks.py`.
- `backend/routers/notebooks.py` (NEW) — CRUD + add/remove members (by `message_id` or `youtube_id`) + `GET /notebooks/messages/available` + `notebook_message_ids()` helper.
- `hybrid_search.py` `message_ids` filter (3 paths); `search.py` `notebook_id` on `/search`, `/ask`, `/ask/advanced` via `_resolve_filters` (empty notebook → 0 results via `-1` sentinel).
- **Verified live**: create notebook → add sermon → scoped search returned ONLY that sermon's chunks; empty notebook → 0 results.

## What's Next (next session starts here)

1. **Finish Phase 1C — Notebooks UI** (the remaining piece). Build `frontend/app/notebooks/page.tsx` (create/list notebooks, add videos from `GET /api/v1/notebooks/messages/available`, view members) + a **notebook selector** on `frontend/app/search/page.tsx` that passes `notebook_id` (all backend endpoints already accept it). Add a nav link (check `frontend/app/layout.tsx` / `FloatingMenu.tsx` — both are session-19 untracked files).
2. **Run Phase 0A → 0B to fill the corpus** (this is what gives all the above real data):
   - **0A**: dashboard → **Translate pending** (API cost: OpenRouter Haiku; ~hundreds of videos). Or it may already be running — counts are climbing.
   - **0B** (after 0A): `cd backend; venv\Scripts\python -m scripts.build_chunks; venv\Scripts\python -m scripts.embed_all_chunks`. Now that migration 005 is applied + `build_chunks` writes `start_time`, the rebuilt corpus gets jump-to-timestamp. (Existing 59 chunks stay timeless — `build_chunks` skips messages that already have chunks; ignore, they're a tiny test set.)
3. **Phase 3 — Dubbing** (greenfield, after the above). edge-tts (installed) + ffmpeg (on PATH) → synced English audio track from `en_final` + per-segment `start_time`/`duration`. See plan.

**Full 4-phase plan**: `C:\Users\kawin\.claude\plans\for-this-project-i-snappy-chipmunk.md`

**Test command to verify pickup works**:
```powershell
cd backend; .\venv\Scripts\uvicorn.exe main:app --port 3001
# then: GET http://localhost:3001/api/v1/notebooks  → []  (proves 1C wired)
```

## Blockers
- **Decision needed — commits.** Nothing is committed and the tree mixes session-19 + session-21 work in `main.py` / `models.py` / `frontend/app/dashboard/`. A clean "commit only s21" isn't possible for those 3 files without untangling s19 first. Options: (a) commit s19 base first (dashboard/glossary/responsive-UI as ~3 commits per s19 handoff), then s21 as `feat(translate): bulk translate runner + dashboard` and `feat(rag): advanced RAG UI + timestamped citations + notebooks`; or (b) one big batch commit. Needs Rakshan's call.
- Bulk translate (0A) is **real OpenRouter API spend** (~hundreds of pending videos). `TRANSLATE_PACE` + Stop button are the throttles.

## Notes for Next Session
- **Migrations 005 + 006 are applied to local dev.db only.** Fresh DB / other machine → `alembic upgrade head` first or models won't match.
- The SQLite `hybrid_search` bugfix materially improves local search correctness — worth keeping even independent of the timestamp feature.
- Advanced RAG synthesis (`/ask/advanced`) calls OpenRouter — costs a little per question. `/search` is free (no LLM) and was used for all verification.
- DB counts are moving (315 fetched, 7,177 en_final at handoff time) — something (ingest and/or a translate run) is progressing in the background.

---

## Pickup Protocol
1. `git checkout codex/youtube-auto-translate-fallback`
2. Read this handoff + the plan file (path above)
3. `cd backend; .\venv\Scripts\python.exe -m alembic upgrade head` (no-op if already 006)
4. Start backend `cd backend; .\venv\Scripts\uvicorn.exe main:app --port 3001`; start frontend `cd frontend; npm run dev`
5. Decide commit strategy (see Blockers) before adding more
6. Continue with Notebooks UI, or kick off 0A/0B to fill the corpus
